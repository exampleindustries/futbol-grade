name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: TypeScript type-check
        run: npm run check

      - name: Build
        env:
          VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
          VITE_SUPABASE_ANON_KEY: ${{ secrets.VITE_SUPABASE_ANON_KEY }}
        run: npm run build

  deploy-cloudflare:
    needs: build
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: TypeScript type-check
        run: npm run check

      - name: Build
        env:
          VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
          VITE_SUPABASE_ANON_KEY: ${{ secrets.VITE_SUPABASE_ANON_KEY }}
        run: npm run build

      - name: Deploy to Cloudflare Workers
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}

  lighthouse:
    needs: build
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4

      - name: Wait for Railway deploy
        if: github.event_name == 'push'
        run: |
          echo "Waiting 90s for Railway to build and deploy..."
          sleep 90
          curl -sf "${{ secrets.RAILWAY_PUBLIC_URL }}/api/health" || true

      - name: Lighthouse audit
        id: lighthouse
        uses: treosh/lighthouse-ci-action@v12
        with:
          urls: |
            ${{ secrets.RAILWAY_PUBLIC_URL }}
            ${{ secrets.RAILWAY_PUBLIC_URL }}/coaches
            ${{ secrets.RAILWAY_PUBLIC_URL }}/auth/login
          runs: 1
          configPath: .github/lighthouse/lighthouserc.json

      - name: Report scores
        if: always()
        run: |
          echo '${{ steps.lighthouse.outputs.manifest }}' | python3 -c "
          import sys, json

          THRESHOLDS = {
              'performance': 50,
              'accessibility': 70,
              'best-practices': 60,
              'seo': 70,
          }

          data = json.load(sys.stdin)
          for entry in data:
              url = entry.get('url', 'unknown')
              summary = entry.get('summary', {})
              scores = {
                  'performance': int(summary.get('performance', 0) * 100),
                  'accessibility': int(summary.get('accessibility', 0) * 100),
                  'best-practices': int(summary.get('best-practices', 0) * 100),
                  'seo': int(summary.get('seo', 0) * 100),
              }
              warnings = [f'{k}: {v} < {THRESHOLDS[k]}' for k, v in scores.items() if v < THRESHOLDS[k]]
              status = '⚠️' if warnings else '✅'
              print(f'{status} {url}')
              for k, v in scores.items():
                  flag = ' ← LOW' if v < THRESHOLDS[k] else ''
                  print(f'   {k}: {v}{flag}')
          print()
          print('Lighthouse scores reported (non-blocking).')
          " || echo "Could not parse Lighthouse results"
